#pragma once

#include <tuple>

namespace Algorithms::DivideAndConquer
{
	template <Arithmetic T>
	std::tuple<std::size_t, std::size_t, T> maxSumSubarray(std::span<T> elements, AlgorithmType algorithm)
	{
		// TODO: Implementation
		return std::make_tuple(0, 0, 0);
	}
}
